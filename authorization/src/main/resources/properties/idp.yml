idp:
  security:
    login-url: /sign-in
    oauth2:
      authorization-server:
        jwt:
          issuer-uri: http://localhost:8080
        jks:
          location: classpath:/keystores/idp-test.jks
          store-pass: 123456!
          alias: identity-provider